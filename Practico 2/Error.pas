
Program Error (input, output);

Var 
     a,b,c:    integer;
     x,y,z:    real;

Begin
     read (x, y, z);
     read (a, b, x);

     //No esta inicializada la variable c


(* read (a, x, y);
     read (b, z, c);

     readln (a, x);
     read (b, c, y, z)
     // estan incializadas todas las variables*)

End.
